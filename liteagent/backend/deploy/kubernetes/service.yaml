apiVersion: v1
kind: Service
metadata:
  name: liteagent-api
  namespace: liteagent
  labels:
    app.kubernetes.io/name: liteagent
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP
  selector:
    app.kubernetes.io/name: liteagent
    app.kubernetes.io/component: api

---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: liteagent
  namespace: liteagent
  labels:
    app.kubernetes.io/name: liteagent
